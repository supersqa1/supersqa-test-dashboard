
stages:
  - firstStage
  - secondStage


my_job_1:
  stage:
    firstStage
  script:
    - echo "job 1"
    - ls
#    - ssh -i $DRP3_KEY
    - scp -i -r $DRP3_KEY ./app root@198.199.111.109:/root/projects/automation_dashboard/
    - ssh -i $DRP3_KEY "root@198.199.111.109 "python3 /root/projects/automation_dashboard/app/app.py"
#    - scp -r ./app ${DASHBOARD_USER}@${DASHBOARD_HOST}:/

my_job_2:
  stage:
    secondStage
  script:
    - echo "foo bar"